{
  "name": "funky-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate-manifest": "node scripts/generate-manifest.js",
    "dev": "node scripts/generate-manifest.js --watch & vite",
    "build": "npm run generate-manifest && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.97.0",
    "chess.js": "^1.4.0",
    "dompurify": "^3.3.1",
    "matter-js": "^0.20.0"
  }
}
